spring:
  application:
    name: academic-service
  datasource:
    url: jdbc:postgresql://localhost:5432/${ACADEMIC_DB_NAME:academic_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:123456}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8082
